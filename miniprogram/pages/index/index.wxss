@import "../../app.wxss";

button {
  border: none;
  line-height: normal;
}

button::after {
  border: none;
}

.about-link {
  display: block;
  text-align: center;
  margin: 12rpx 0 6rpx;
  font-size: 22rpx;
  color: rgba(242, 245, 255, 0.5);
}
